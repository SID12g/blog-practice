/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath:"/sid12g-blog",
    images: {
        domains: ["localhost", "*"],
      },
      reactStrictMode: true,
}

module.exports = nextConfig
